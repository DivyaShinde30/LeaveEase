{
  "name": "leaveease",
  "version": "1.0.0",
  "description": "LeaveEase — leave management (Express + MongoDB + static frontend)",
  "main": "backend/server.js",
  "scripts": {
    "start": "npm --prefix backend start",
    "dev": "npm --prefix backend run dev",
    "install:backend": "npm install --prefix backend",
    "test": "echo \"No root tests — use backend/start\" && exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "mongoose": "^9.2.4",
    "multer": "^2.1.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
